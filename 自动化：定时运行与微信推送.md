# 自动化：定时运行与微信推送

日期：2026-08-10
标签：#自动化 #投研系统 #Server酱 #launchd

## 这一层解决什么

原来的问题：`fetch_fred_snapshot.py` 和 `fetch_flows_snapshot.py` 都能跑，但

- 要手动敲命令，所以实际上停在 2026-06-17
- `build_alerts()` 算出的告警只 `print` 到终端，没人看
- 没有任何"今天到底跑没跑成"的记录

这三件事合起来的后果是：**系统不存在，只有脚本存在。**

现在补上的是中间那一层：调度 → 执行 → 告警出口 → 留痕。

## 新增文件

| 文件 | 作用 |
|---|---|
| `Scripts/notify.py` | Server酱推送模块。当天去重、失败不抛异常 |
| `Scripts/run_daily.py` | 编排：跑两个脚本 → 收告警 → 推送 → 写日志 |
| `Scripts/run_daily.sh` | launchd 入口，负责设好 PATH |
| `Scripts/launchd/com.tera.toushi.daily.plist` | 每日 07:30 定时 |
| `Scripts/.secrets.example.json` | SendKey 模板 |
| `.gitignore` | 排除密钥与原始 CSV |

## 三步启用

### 1. 配置 SendKey

去 [sct.ftqq.com](https://sct.ftqq.com) 微信扫码登录，拿到 SendKey，然后：

```bash
cd "/Users/tera/Desktop/My pages/ObsidianVault/00 投研系统/Scripts"
cp .secrets.example.json .secrets.json
# 编辑 .secrets.json，把 key 填进去
```

验证：

```bash
python3 "00 投研系统/Scripts/notify.py" --status   # 看有没有读到 key
python3 "00 投研系统/Scripts/notify.py" --test     # 真发一条，看微信
```

### 2. 手动跑一次完整流程

```bash
cd "/Users/tera/Desktop/My pages/ObsidianVault"
python3 "00 投研系统/Scripts/run_daily.py" --force
```

看三个地方：`Reports/` 有没有新快照、`Logs/runs/2026-08.md` 有没有新行、微信有没有响。

### 3. 装定时任务

```bash
cp "/Users/tera/Desktop/My pages/ObsidianVault/00 投研系统/Scripts/launchd/com.tera.toushi.daily.plist" ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.tera.toushi.daily.plist
launchctl list | grep toushi          # 确认已加载
```

卸载：`launchctl unload ~/Library/LaunchAgents/com.tera.toushi.daily.plist`

改时间：编辑 plist 里的 `Hour`/`Minute`，然后 unload 再 load。

## 推送规则

只有这三种情况会响：

1. **触发告警** — `build_alerts()` 的四条规则命中（双收缩 / 稳定币拐点 / ETF 急流出 / 杠杆过热）
2. **抓取失败** — 沉默的定时任务和没装是一回事，失败必须说话
3. **心跳** — 只有加 `--heartbeat` 才发

其余情况一律安静。噪音一多你就会开始忽略它，那这套东西就白做了。

## 几个设计决定

**为什么 `RunAtLoad` 设 true。**
笔记本在 07:30 睡着时 launchd 会直接跳过这次触发，不补。所以同时开 `RunAtLoad`，登录或唤醒后补跑一次。重复推送由 `run_daily.py` 里的「当天已成功」守卫挡掉（`Data/run_state.json`）。

**为什么告警要按内容去重而不是按次数。**
指纹取的是「标题 + 正文」的哈希，存在 `Data/notify_state.json`，按天重置。所以同一条【双收缩】当天推一次，第二天条件仍然成立会再推一次——这是对的，持续的告警值得每天提醒。

**为什么 `latest.json` 进 git 而原始 CSV 不进。**
CSV 大且可再生。`latest.json` / `alerts.json` 每天一次 diff，就是这套系统在持续运行的时间线，也是以后写复盘唯一可信的素材。

**为什么推送模块永不抛异常。**
数据抓取比通知重要。Server酱挂了不应该导致当天快照没生成。

## 排查

| 症状 | 先看 |
|---|---|
| 定时没跑 | `Logs/launchd.err.log`、`launchctl list \| grep toushi` |
| 跑了没推送 | `python3 Scripts/notify.py --status` 看 key；再看 `Logs/runs/` 当天行 |
| 推送重复 | `Data/notify_state.json` 的 date 字段是不是没更新 |
| 想强制重跑 | 加 `--force` |
| 不想真推送 | 加 `--dry-run` |

## 待办

- [ ] 配 SendKey，跑通第一次真实推送
- [ ] 装 launchd，连续观察 3 天日志
- [ ] `fetch_fred_snapshot.py` 目前没有 `build_alerts()`，利率层还没有告警规则——补上
- [ ] 考虑让 `run_daily.py` 跑完自动 `git commit`，让运行痕迹进版本历史

## 相关

- [[自动化Demo：FRED利率与通胀快照]]
- [[资金流层：口径与用法]]
- [[数据获取方案]]
