# Polymarket观测站：霍尔木兹与美联储

日期：2026-08-12
标签：#事件雷达 #Polymarket #霍尔木兹 #FOMC #自动化

## 现在监控什么

| 事件 | 雷达显示的概率 | 裁决来源 |
|---|---|---|
| 霍尔木兹海峡风险 | 截至 2026-08-31，IMF PortWatch 的7日平均到港船次仍未达到60的市场隐含概率 | IMF PortWatch |
| 美联储利率决策 | 2026年9月FOMC会议“降息 / 不变 / 加息”的完整市场隐含概率分布 | FOMC会后声明 |

这里的数据来自 **Polymarket** 公开市场价格。展示的是市场形成的**隐含概率**，不是客观预测，也不构成交易建议。客户通知固定标注数据来源和北京时间的采集时间，并明确分成两层：**Polymarket 负责“市场怎么押”**；IMF PortWatch 或 FOMC 会后声明负责**“最后发生了什么”**。

对外产品名为 **Polymarket观测站**，副标题为“事件概率与事实判定”。页面和推送固定声明：这是非官方观测工具，与 Polymarket 无隶属或合作关系。

霍尔木兹事件不把 IMF PortWatch 写成泛泛的“官方数据”，而是写成“最终判定指标”：监测霍尔木兹海峡7日平均通行船次，达到60即视为恢复正常。这样客户不会把 Polymarket 的市场概率误认为 IMF 的预测。

美联储事件直接向客户展示五个互斥结果：降息至少0.50个百分点、降息0.25个百分点、维持利率不变、加息0.25个百分点、加息至少0.50个百分点。系统用各市场买卖价中点计算原始概率，再归一化并使用总和校正的四舍五入，保证客户看到的五项合计为100%。“不降息”不会作为单独结果展示，因为它会把维持不变和加息两个方向相反的结果混在一起。

## 为什么不是只看最新价格

事件雷达采用双轨推送：

- 每天北京时间 08:00 固定发送一次包含两个事件的完整晨报；
- 后台仍每15分钟检查一次，其余时间只有在以下条件满足时才打断用户：

- 任一结果1小时变化至少5个百分点；
- 任一结果24小时变化至少10个百分点；
- 任一结果持续两个采样点穿越25%、50%或75%；
- 美联储事件的领先结果持续两个采样点发生变化；
- 底层裁决规则发生变化；
- 市场停止接受订单，可能已经裁决。

价格触发还必须通过质量门槛：总流动性至少5万美元、单个分组买卖价差不超过5个百分点；美联储五项原始概率合计还必须位于90%–110%。相同事件告警后冷却6小时，除非任一结果又移动至少5个百分点。

晨报发送窗口为北京时间 08:00–08:59。系统运行在日本电脑上，因此 launchd 在日本时间09:00增加一次明确触发。窗口内只有在两个事件都成功抓取后才发送，并按北京时间日期去重；如果异常恰好在晨报时间发生，会合并到同一条晨报，不会连续推送两条。电脑在整个窗口内休眠时，当天晨报跳过，不会在下午补发过时信息。

## 运行与证据

- 配置：`Config/event_watchlist.json`
- 监控器：`Scripts/event_radar.py`
- 当前看板：`Reports/事件概率雷达.md`
- 原始审计记录：`Data/events/history/YYYY-MM.jsonl`
- 运行状态：`Data/events/state.json`

前台看板和高频状态属于可再生运行数据，不进入 Git；触发规则、精选事件和测试进入版本历史。

手动只读预览：

```bash
cd "/Users/tera/Projects/toushi-system"
.venv/bin/python3 Scripts/event_radar.py --dry-run
```

## 推送与公开分享

微信推送只保留客户做判断需要的信息，顺序固定为：

1. 市场现在怎么押；
2. 相比此前发生了什么，以及为什么触发提醒；
3. 最终依据什么指标判定；
4. 数据来源与北京时间；
5. 风险声明。

买卖价差、流动性和24小时成交额仍参与系统的质量门槛并保留在内部报告，但不占用客户推送的首屏空间。

系统每次正常运行也会在 `Reports` 生成两份不含密钥的可再生文件：

- `事件概率雷达·公开快照.html`：博客公开页面；
- `事件概率雷达·分享卡片.html`：1080×1350 朋友圈卡片源文件。

导出到当前博客并生成 PNG：

```bash
cd "/Users/tera/Projects/toushi-system"
bash Scripts/export_event_share.sh
```

这个命令只刷新分享文件，不发送微信通知，也不改变雷达的监控状态。它会更新博客的 `/event-radar/` 页面和 `/images/event-radar-latest.png` 图片；是否提交并发布由人工确认。

公开页面和图片已经发布后，`public_share_enabled` 设为 `true`。每天晨报通过 Markdown 嵌入固定公网图片地址，并用数据时间作为查询参数避免微信沿用旧缓存；异常即时提醒仍以文字和公开页面链接为主，避免引用早晨的旧图片。

## 边界

- 不连接钱包；
- 不创建订单；
- 不处理充值或结算；
- 不提供绕过地区限制的方法；
- 微信只发送事件信息，不发送交易建议。
